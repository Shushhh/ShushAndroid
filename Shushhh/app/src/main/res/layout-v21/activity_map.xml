<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/map"
        tools:context=".MapsActivity"
        android:name="com.google.android.gms.maps.SupportMapFragment" />
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:elevation="10dp"
        android:background="@drawable/background_round"
        android:id="@+id/location_searchbar">

        <ImageView
            android:id="@+id/magnify_button"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/ic_magnify"
            app:layout_constraintLeft_toLeftOf="@id/location_searchbar"
            app:layout_constraintTop_toTopOf="@id/location_searchbar"
            app:layout_constraintBottom_toBottomOf="@id/location_searchbar"
            android:layout_marginStart="10dp"
            android:contentDescription="TODO" />

        <EditText
            android:id="@+id/search_textfield"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:autofillHints="location"
            android:hint="Enter Address, City or Zip Code"
            android:inputType="textAutoCorrect"
            android:textColor="#000"
            android:textSize="15sp"
            app:layout_constraintBottom_toBottomOf="@id/location_searchbar"
            app:layout_constraintLeft_toRightOf="@id/magnify_button"
            app:layout_constraintRight_toRightOf="@id/location_searchbar"
            app:layout_constraintTop_toTopOf="@id/location_searchbar"
            android:imeOptions="actionSearch"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>